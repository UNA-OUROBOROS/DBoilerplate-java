[database]
    # example SQLite connection
    schema = "DBoilerplate"
    host = "localhost"
    port = 3306
    [database.credentials]
        user = "admin"
        password = "admin"
[unit-tests]
    ommit_db_tests = true
    [unit-tests.database]
        # in memory database for SQLite
        schema = ":memory:"
        # in SQLite there is no host and port
        host = ""
        port = 0
        # also there is no user and password
        [unit-tests.database.credentials]
            user = ""
            password = ""